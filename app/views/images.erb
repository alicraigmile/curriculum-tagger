<!DOCTYPE html>
<html>
<head>
  <title>Images</title>
  <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>

  <h1>Images</h1>
  <% for image in @images %>
    <div class="box">
      <h2><a href="<%= image.url %>"><%= image.title %></a></h2>
      <img src="<%= image.url %>" width="<%= image.width %>" height="<%= image.height %>">
    </div>
   <% end %>
  
<%= include 'github-ribbon.erb' %>
</body>
</html>
